import {unstable_createMuiStrictModeTheme as createTheme, responsiveFontSizes} from '@material-ui/core'
import NeonFontFile from './fonts/neon-font/Neon.ttf'
import KlaxonsFontFile from './fonts/Klaxons.otf'
import NeoNeonFontFile from './fonts/Neoneon.otf'
import designerFont from './fonts/Designer-Font-Family/Designer.otf'
import designerBoldFont from './fonts/Designer-Font-Family/Designer-Bold.otf'
import SortDecaiScriptFontFile
  from './fonts/Swistblnk Sortdecai Cursive Script and Bonus/Swistblnk Sortdecai Cursive Script and Bonus/Sortdecai Cursive Script.ttf'
import grafterFont from './fonts/Grafter/Grafter.otf'
import growlFont from './fonts/Growl/fonts/Growl_rounded.otf'
import margotFont from './fonts/Margot/Margot-Regular.ttf'
import buildingFont from './fonts/Building/Building_Typeface.ttf'
import highTideFont from './fonts/High Tide/HighTide.otf'
import metricaFont from './fonts/Metrica/Metrica-Regular.otf'
import neythalFont from './fonts/Neythal/neythal-regular.ttf'
import nickainleyFont from './fonts/Nickainley/TTF/Nickainley.ttf'
import oranienbaumFont from './fonts/Oranienbaum/Oranienbaum.ttf'
import smaqFont from './fonts/Smaq/alt_smaq_6_8.ttf'
import afectaFont from './fonts/Smaq/alt_smaq_2_8.ttf'
import akumaFont from './fonts/AkumaFF/TTF/Akuma.ttf'
import amtenarFont from './fonts/Amtenar/Amtenar font web/Amtenar.ttf'
import andersFont from './fonts/AndersFF/Anders.ttf'
import badheadFont from './fonts/Badhead Typeface/Badhead Typeface.otf'
import barbaroFont from './fonts/BarbaroFF/barbaro.ttf'
import beatsFont from './fonts/Beats OTF (web FONT)/beats web font/beats-webfont.ttf'
import bhaversFont from './fonts/Bhavers-Typeface/Bhavers_Typeface/Bhavers_Typeface.otf'
import billowFont from './fonts/Billow Font/Billow.ttf'
import birchwoodFont from './fonts/BirchwoodFF/Birchwood/Birchwood Regular.ttf'
import birchwoodBoldFont from './fonts/BirchwoodFF/Birchwood/Birchwood Bold.ttf'
import bohoFont from './fonts/Boho-Familyff/Boho Script.otf'
import bohoDingbatsFont from './fonts/Boho-Familyff/Boho Dingbats.otf'
import bohoOrnamentsFont from './fonts/Boho-Familyff/Boho Ornaments.otf'
import brilantFont from './fonts/BrilantFF/Brilant.ttf'
import chocolateFont
  from './fonts/Chocolate Heart Script/Chocolate Heart Script/Chocolate Heart Script.otf'
import cornishFont from './fonts/Cornish + Extra/Cornish.otf'
import evelynFont from './fonts/Evelyn + Extra/Evelyn_regular.otf'
import exoticusFont from './fonts/Exoticus font/Exoticus Font Web/fonts/Exoticus.ttf'
import fireflyFont from './fonts/Firefly/SEANCo-Firefly-2015.otf'
import danzeFont from './fonts/Fresh-Food-CollectionFF/Danze/Danze.ttf'
import fryeFont from './fonts/Fresh-Food-CollectionFF/Frye/Frye.ttf'
import fryeOrnamentsFont from './fonts/FryeFF/Frye Ornaments.ttf'
import pictomiffFont from './fonts/Fresh-Food-CollectionFF/Pictomiff/Pictomiff.ttf'
import gooniesFont from './fonts/Goonies/Goonies.ttf'
import grothikaFont from './fonts/GrothikaFF/Grothika-Regular.otf'
import handtersFont from './fonts/Handters-typeface/Handters typeface OTF/Handters Typeface.otf'
import hawaiiFont from './fonts/HawaiiFF/Hawaii/OTF/Hawaii.otf'
import heatherFont from './fonts/Heather-Typeface/Heather.otf'
import helenaFont from './fonts/Helena-Font/Helena/OTF/Helena.otf'
import humblleFont from './fonts/Humblle + Extra/humblle.otf'
import infamyFont from './fonts/Infamy-Family-ff/Infamy-Outline.otf'
import iorekByrnisonFont from './fonts/Iorek Byrnison/Iorek Byrnison.otf'
import jeumpaScriptFont from './fonts/jeumpa script/Jeumpa Script.otf'
import engineFont from './fonts/L_ Engineer/LEngineer-Regular.otf'
import laskerFont from './fonts/Laskar/Format/OTF/Laskar Script.otf'
import lemonadeFont from './fonts/Lemonade Font/Lemonade font.ttf'
import matadorFont from './fonts/Matador/Matador/Matador.otf'
import momFont from './fonts/MomFF - HOT/Mom-Outline.ttf'
import monthoersFont from './fonts/Monthoers/Monthoers.otf'
import nadhineFont from './fonts/Nadhine Script/Nadhine Script.otf'
import newTheoryFont from './fonts/New-Theoryff/NewTheory.ttf'
import oliveFont from './fonts/Oliveff/Olive.otf'
import peintureFont from './fonts/Peinture Typeface/Peinture.otf'
import piconsFont from './fonts/Picons-Font/Web font/Picons-Font.ttf'
import rawFont from './fonts/Raw-Fontff/Raw.ttf'
import realistFont from './fonts/Realist/format/Realist.otf'
import rideMyBikeFont from './fonts/Ride-My-Bike-Serif-Pro-Family-ff/RidemyBikeSerifPro-Regular.otf'
import rideMyBikeOrnamentsFont from './fonts/Ride-My-Bike-Serif-Pro-Family-ff/RidemyBike-Ornaments.otf'
import saboresFont from './fonts/Sabores-Script-Familyff/SaboresScript-Regular/SaboresScript-Regular.otf'
import saboresDingbatsFont from './fonts/Sabores-Script-Familyff/SaboresScript-Dingbats/SaboresScript-Dingbats.otf'
import scratchFont from './fonts/ScratchFF/Scratch-Year 1.otf'
import serenScriptFont from './fonts/Seren-Script/Seren-Script-Heavy.otf'
import showcaseFont from './fonts/Showcase-Family-50-Off/Showcase-Family/ShowcaseScript.otf'
import showcaseOrnamentsFont from './fonts/Showcase-Family-50-Off/Showcase-Family/ShowcaseOrn.otf'
import splandorFont from './fonts/Splandor-Typeface/Splandor typeface/Splandor typeface/splandor typeface.otf'
import springtimeFont from './fonts/Springtime/Springtime.otf'
import springtimeOrnamentsFont from './fonts/Springtime/SpringtimeOrnaments.otf'
import standlyFont from './fonts/standly/standly/Standly web font/Standly.otf'
import starshineFont from './fonts/Starshine-script/Starshine-Regular.otf'
import steelmondFont from './fonts/Steelmond/Steelmond.otf'
import sweetheartFont from './fonts/Sweethart Font/Fonts/Sweethart.otf'
import tomahawkFont from './fonts/Tomahawk-—-Vintage-Font/Tomahawk.otf'
import triumpFont from './fonts/Triump-Familyff/Triump-Regular/Triump-Regular.otf'
import ttMastersFont from './fonts/TT Masters/Masters Opentype/TT Masters Thin.otf'
import twiceFont from './fonts/Twice/twice.otf'
import wildYouthFont from './fonts/Wild-Youth-Typeface/WildYouth-Regular.otf'
import elphFont from './fonts/elph_chubba/chubhand.ttf'
import monitoricaFont from './fonts/IPHostMonitor-Font/OT-tt/Monitorica-Rg.ttf'
import loveloFont from './fonts/LoveloLineLight/Lovelo Black.otf'
import loveloFontLine from './fonts/LoveloLineLight/Lovelo Line Bold.otf'
import loveloFontLineLight from './fonts/LoveloLineLight/Lovelo Line Light.otf'
import stencilFont from './fonts/StencilStd.otf'
import monotonFont from './fonts/Monoton/monoton/Monoton-Regular.ttf'
import marskeFont from './fonts/Marske/Marske.otf'

export type FontFace = {
  fontDisplay: any
  fontFamily: any
  fontStyle: any
  fontWeight: number
  src: string
}

export const iconFonts: FontFace[] = [{
  fontFamily: 'Picons Font',
  fontStyle: 'normal',
  fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
  fontWeight: 400,
  src: `
    local('Picons Font'),
    url(${piconsFont}) format('opentype')
  `
},{
  fontFamily: 'Boho Dingbats',
  fontStyle: 'normal',
  fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
  fontWeight: 400,
  src: `
    local('Boho Dingbats'),
    url(${bohoDingbatsFont}) format('opentype')
  `
},{
  fontFamily: 'Boho Ornaments',
  fontStyle: 'normal',
  fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
  fontWeight: 400,
  src: `
    local('Boho Ornaments'),
    url(${bohoOrnamentsFont}) format('opentype')
  `
},{
  fontFamily: 'Frye Ornaments',
  fontStyle: 'normal',
  fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
  fontWeight: 400,
  src: `
    local('Frye Ornaments'),
    url(${fryeOrnamentsFont}) format('truetype')
  `
}, {
    fontFamily: 'Ride My Bike Ornaments',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Ride My Bike Ornaments'),
    url(${rideMyBikeOrnamentsFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Sabores Dingbats',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Sabores Dingbats'),
    url(${saboresDingbatsFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Showcase Ornaments',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Showcase Ornaments'),
    url(${showcaseOrnamentsFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Springtime Ornaments',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Springtime Ornaments'),
    url(${springtimeOrnamentsFont}) format('opentype')
  `
  }
]

export const getIconFont = (fontFamily: string) =>{
  const result = iconFonts.filter((iconFontFace:FontFace) =>{
    return iconFontFace.fontFamily === fontFamily
  })

  if(result[0]){
    return result[0]
  } else {
    return undefined
  }
}

export const getFontFace = (fontFamily: string) =>{
  const result = allFontFaces.filter((fontFace:FontFace) =>{
    return fontFace.fontFamily === fontFamily
  })

  if(result[0]){
    return result[0]
  } else {
    return allFontFaces[0]
  }
}

export const allFontFaces: FontFace[] = [
  {
    fontFamily: 'Designer-Bold',
    fontStyle: 'bold',
    fontDisplay: '', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 700,
    src: `
    local('designer-bold'),
    url(${designerBoldFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Designer-Regular',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('desinger'),
    url(${designerFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Stencil',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Stencil'),
    url(${stencilFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Helena Regular',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Helena Regular'),
    url(${helenaFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Birchwood',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Birchwood'),
    url(${birchwoodFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Monoton',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Monoton'),
    url(${monotonFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Elph',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Elph'),
    url(${elphFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Monitorica',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Monitorica'),
    url(${monitoricaFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Lovelo Black',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Lovelo Black'),
    url(${loveloFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Lovelo Line Light',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Lovelo Line Light'),
    url(${loveloFontLineLight}) format('opentype')
  `
  },
  {
    fontFamily: 'Lovelo Line Bold',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Lovelo Line Bold'),
    url(${loveloFontLine}) format('opentype')
  `
  },
  {
    fontFamily: 'Marske',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Marske'),
    url(${marskeFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Barbaro',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Barbaro'),
    url(${barbaroFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Beats',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Beats'),
    url(${beatsFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Bhavers',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Bhavers'),
    url(${bhaversFont}) format('opentyoe')
  `
  },
  {
    fontFamily: 'Billow',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Billow'),
    url(${billowFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Birchwood Bold',
    fontStyle: 'bold',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 700,
    src: `
    local('Birchwood Bold'),
    url(${birchwoodBoldFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Boho Script',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Boho Script'),
    url(${bohoFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Brilant',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Brilant'),
    url(${brilantFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Chocolate Heart',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Chocolate Heart'),
    url(${chocolateFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Cornish',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Cornish'),
    url(${cornishFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Evelyn',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Evelyn'),
    url(${evelynFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Exoticus',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Exoticus'),
    url(${exoticusFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Firefly',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Firefly'),
    url(${fireflyFont}) format('opentype')
  `
  }
  , {
    fontFamily: 'Neythal',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Neythal'),
    url(${neythalFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Nickainley',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Nickainley'),
    url(${nickainleyFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Smaq',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Smaq'),
    url(${smaqFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Oranienbaum',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Oranienbaum'),
    url(${oranienbaumFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Building',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Building'),
    url(${buildingFont}) format('truetype')
  `
  },
  {
    fontFamily: 'High Tide',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('High Tide'),
    url(${highTideFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Metrica',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Metrica'),
    url(${metricaFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Margot',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Margot'),
    url(${margotFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Growler',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Growler'),
    url(${growlFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Grafter',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Grafter'),
    url(${grafterFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Designer-Bold',
    fontStyle: 'bold',
    fontDisplay: '', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 700,
    src: `
    local('designer-bold'),
    url(${designerBoldFont}) format('opentype')
  `
  },
  {
    fontFamily: 'NEON Regular',
    fontStyle: 'bold',
    fontDisplay: 'auto', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 500,
    src: `
    local('NEON Regular'),
    url(${NeonFontFile}) format('truetype')
  `
    // unicodeRange:
    //   'U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+2000-206F, U+2074, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF',
  },
  {
    fontFamily: 'Klaxons',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Klaxons'),
    url(${KlaxonsFontFile}) format('opentype')
  `
  },
  {
    fontFamily: 'Neo Neon',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Neo Neon'),
    url(${NeoNeonFontFile}) format('opentype')
  `
  },
  {
    fontFamily: 'Sortdecai Cursive Script',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Sortdecai Cursive Script'),
    url(${SortDecaiScriptFontFile}) format('truetype')
  `
  },
  {
    fontFamily: 'Afecta',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Afecta'),
    url(${afectaFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Akuma',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Akuma'),
    url(${akumaFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Amtenar',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Amtenar'),
    url(${amtenarFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Anders',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Anders'),
    url(${andersFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Badhead Typeface',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Badhead Typeface'),
    url(${badheadFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Danze',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Danze'),
    url(${danzeFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Pictomiff',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Pictomiff'),
    url(${pictomiffFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Frye',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Frye'),
    url(${fryeFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Goonies',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Goonies'),
    url(${gooniesFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Grothika',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Grothika'),
    url(${grothikaFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Handters',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Handters'),
    url(${handtersFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Hawaii',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Hawaii'),
    url(${hawaiiFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Heather Typeface',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Hawaii'),
    url(${heatherFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Humblle',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Humblle'),
    url(${humblleFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Infamy',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Infamy'),
    url(${infamyFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Iorek Byrnison',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Iorek Byrnison'),
    url(${iorekByrnisonFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Jeumpa Script',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Jeumpa Script'),
    url(${jeumpaScriptFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Engine',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Engine'),
    url(${engineFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Lasker Script',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Lasker Script'),
    url(${laskerFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Lemonade',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Lemonade'),
    url(${lemonadeFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Matador',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Matador'),
    url(${matadorFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Mom',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Mom'),
    url(${momFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Monthoers',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Monthoers'),
    url(${monthoersFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Nadhine Script',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Nadhine Script'),
    url(${nadhineFont}) format('opentype')
  `
  },
  {
    fontFamily: 'New Theory',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('New Theory'),
    url(${newTheoryFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Olive',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Olive'),
    url(${oliveFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Peinture',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Peinture'),
    url(${peintureFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Raw Font',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Raw Font'),
    url(${rawFont}) format('truetype')
  `
  },
  {
    fontFamily: 'Realist',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Realist'),
    url(${realistFont}) format('opentype')
  `
  },
  {
    fontFamily: 'RidemyBike Serif Pro',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('RidemyBike Serif Pro'),
    url(${rideMyBikeFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Sabores Script',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Sabores Script'),
    url(${saboresFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Scratch Year One',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Scratch Year One'),
    url(${scratchFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Seren Script',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Seren Script'),
    url(${serenScriptFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Showcase Script',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Showcase Script'),
    url(${showcaseFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Splandor Typeface',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Splandor Typeface'),
    url(${splandorFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Springtime',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Springtime'),
    url(${springtimeFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Standly',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Standly'),
    url(${standlyFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Starshine',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Starshine'),
    url(${starshineFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Steelmond',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Steelmond'),
    url(${steelmondFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Sweethart',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Sweethart'),
    url(${sweetheartFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Tomahawk',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Tomahawk'),
    url(${tomahawkFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Triump',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Triump'),
    url(${triumpFont}) format('opentype')
  `
  },
  {
    fontFamily: 'TT Masters',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('TT Masters'),
    url(${ttMastersFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Twice',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Twice'),
    url(${twiceFont}) format('opentype')
  `
  },
  {
    fontFamily: 'Wild Youth',
    fontStyle: 'normal',
    fontDisplay: 'swap', // uses the fallback font to display the text until the custom font has fully downloaded. This is also known as a “flash of unstyled text” or FOUT.
    fontWeight: 400,
    src: `
    local('Wild Youth'),
    url(${wildYouthFont}) format('opentype')
  `
  },
  ...iconFonts
]

export const fonts: { [key: string]: any } = allFontFaces.reduce((accumulator: {[key:string]:any}, currentValue: FontFace) => {
  return {
    ...accumulator,
    [currentValue.fontFamily.replace(' ', '')]: currentValue
  }
},{})

console.log('FOONTS IN THEME', fonts)

export const theFontsString:string[] = allFontFaces.map((font:FontFace)=>{
  return font.fontFamily
})

const fontString = [
  ...theFontsString,
  'sans-serif'
].join(',')

export const NeonBlueHex = '#1900A0'
export const NeonGreenHex = '#38FF12'
export const NeonFuchsiaHex = '#f09'
export const NeonYellowRoseBlueHex = '#FFF100'
export const NeonElectricVioletHex = '#9600FF'
export const NeonAquaHex = '#00F5FB'

export const FacebookBlue = '#1877f2'
export const TwitterBlue = '#1da1f2'
export const InstagramMagenta = '#c32aa3'
export const PinterestRed = '#bd081c'

const NeonTheme = responsiveFontSizes(
  createTheme({
    palette: {
      background: {
        default: '#000000',
        paper: '#FAFAFA'
      },
      primary: {
        main: '#00F5FB'
      },
      secondary: {
        main: '#FF00E3'
      },
      error: {
        main: '#AE2727',
        light: '#D79393',
        dark: '#840E0E'
      },
      success: {
        main: '#27AE60',
        light: '#93D7B0',
        dark: '#0E8433'
      },
      warning: {
        main: '#E2AB1F',
        light: '#F1D58F',
        dark: '#CF800A'
      },
      text: {
        primary: '#FAFAFA',
        secondary: '#6B6B6B'
      }
    },
    typography: {
      fontFamily: fontString,
      h1: {
        // Title1
        ...getFontFace('Designer-Bold'),
        textTransform: 'uppercase',
        fontSize: '44px',
        fontStyle: 'normal',
        fontWeight: 400,
        lineHeight: 1.8,
        letterSpacing: '-0.03em'
      },
      h2: {
        ...getFontFace('Designer-Bold'),
        textTransform: 'uppercase',
        // Title2
        fontSize: '32px',
        fontStyle: 'normal',
        fontWeight: 700,
        lineHeight: 1.3,
        letterSpacing: '-0.03em'
      },
      h3: {
        // Title3
        ...getFontFace('Designer-Bold'),
        textTransform: 'uppercase',
        fontSize: '24px',
        fontStyle: 'normal',
        fontWeight: 600,
        lineHeight: 1.4,
        letterSpacing: '-0.03em'
      },
      h6: {
        // Title3
        ...getFontFace('Designer-Bold'),
        textTransform: 'uppercase',
        fontSize: '1.25rem',
        fontStyle: 'normal',
        fontWeight: 700,
        lineHeight: 1.5,
        letterSpacing: '-0.03em'
      },
      body1: {
        // Body
        ...getFontFace('Designer-Bold'),
        fontSize: '17px',
        fontStyle: 'normal',
        fontWeight: 400,
        lineHeight: 1.5,
        letterSpacing: '-0.03em'
      },
      body2: {
        // Large
        ...getFontFace('Designer-Bold'),
        fontSize: '19px',
        fontStyle: 'normal',
        fontWeight: 600,
        lineHeight: 1.2,
        letterSpacing: '-0.03em'
      },
      button: {
        // Button
        ...getFontFace('Designer-Bold'),
        fontSize: '19px',
        fontStyle: 'normal',
        fontWeight: 700,
        lineHeight: 1,
        letterSpacing: '-0.03em'
      },
      caption: {
        ...getFontFace('Designer-Bold'),
        textTransform: 'uppercase',
        fontSize: '16px',
        fontStyle: 'normal',
        fontWeight: 700,
        lineHeight: 1,
        letterSpacing: '-0.03em'
      },
      subtitle1: {
        // Small
        fontSize: '12px',
        fontStyle: 'normal',
        fontWeight: 400,
        lineHeight: 1.45,
        letterSpacing: '-0.03em'
      },
      subtitle2: {
        // Micro
        fontSize: '8px',
        fontStyle: 'normal',
        fontWeight: 850,
        lineHeight: 1.35,
        letterSpacing: '-0.03em'
      }
    },
    overrides: {
      MuiCssBaseline: {
        '@global': {
          '@font-face': allFontFaces
        }
      },
      MuiToolbar: {
       regular: {
         minHeight: '92px !important'
       }
      }
    }
  })
)

export default NeonTheme
